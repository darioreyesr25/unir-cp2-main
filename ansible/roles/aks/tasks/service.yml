- name: Render Service manifest
  template:
    src: service.yml.j2
    dest: "/tmp/aks-service-{{ front_service_name }}.yaml"

- name: Apply Service manifest
  command: kubectl apply -f /tmp/aks-service-{{ front_service_name }}.yaml
