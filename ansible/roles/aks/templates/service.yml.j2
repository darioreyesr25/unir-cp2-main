---
apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
spec:
  selector:
    app: stackedit
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: {{ service_port }}
    targetPort: 8080
