---
- name: Configure VM and Deploy Podman Container
  hosts: azure_vm
  become: yes
  roles:
    - vm
  vars_files:
      - ./vars.yml
  tasks:
    - name: Confirm playbook completed
      debug:
        msg: "Playbook completed successfully."
